# 11. Reproducibility appendix — table-to-JSON map

Every published metric in §7 and §8 is backed by a committed JSON in `paper/figures/`. The table below maps each reported row to its source file and the exact confidence-interval call used. All CIs are computed by `src/metrics/bootstrap.py` at `seed=42`; the resample count follows the two-tier convention stated in the §7 data-availability note (n_resamples=1500 for small-N tiers with N ≤ 100, n_resamples=100 for the full large public sets). Point estimates labelled "full-resolution" are the `metrics.auc` value stored in the JSON (computed via `roc_curve(..., n_points=200)`); CI central estimates are computed at the bootstrap's internal `n_points=100` and can differ from the full-resolution value in the third–fourth decimal.

| Paper location | Reported number(s) | Backing JSON | CI call |
|---|---|---|---|
| §7.1 CASIA-FASD `minifasnet_only` | ACER 12.67%, EER 12.70%, AUC 0.9452 [0.9366, 0.9560] (full-res 0.9454) | `results_casia_fasd_test_full_minifasnet_only.json` (N=2,408) | `auc_ci(n_resamples=100, seed=42)` |
| §7.1 CASIA-FASD `image_only` / `hybrid` | AUC 0.9139 / 0.9138 | `results_casia_fasd_test_full_{image_only,hybrid}.json` | `auc_ci(n_resamples=100, seed=42)` |
| §7.1 CelebA-Spoof `minifasnet_only` | ACER 28.67%, EER 28.61%, AUC 0.7818 [0.7663, 0.7993] (full-res 0.7820) | `results_celeba_spoof_hf_eval_full_minifasnet_only.json` (N=2,611) | `auc_ci(n_resamples=100, seed=42)` |
| §7.1 CelebA-Spoof `image_only` / `hybrid` | AUC 0.7261 / 0.7246 | `results_celeba_spoof_hf_eval_full_{image_only,hybrid}.json` | `auc_ci(n_resamples=100, seed=42)` |
| §7.1 Kainyyy (N=200, post-fix) | `minifasnet_only` ACER 19.38%, EER 18.12%, AUC 0.8648; `image_only` ACER 36.88%, EER 35.62%, AUC 0.6725 | `results_kainyyy_all_n200_{minifasnet_only,image_only}.json` (N=200, 40 bona-fide / 160 attack) | point estimate (no CI) |
| §7.2 in-house replay (all 3 pipelines) | AUC 0.9245 [0.8576, 0.9812] / 0.9264 [0.8676, 0.9748] | `results_in_house_replay_n100_{minifasnet_only,image_only,hybrid}.json` (N=100) | `auc_ci(n_resamples=1500, seed=42)` |
| §7.3 in-house full `minifasnet_only` | ACER 56.00%, AUC 0.4717 [0.3314, 0.5891] (CI central 0.4781) | `results_in_house_full_n325_minifasnet_only.json` (N=325) | `auc_ci(n_resamples=1500, seed=42)`; point estimate is `metrics.auc` (n_points=200) |
| §7.3 in-house full `image_only` | ACER 56.00%, AUC 0.4008 [0.2840, 0.5262] (CI central 0.4131) | `results_in_house_full_n325_image_only.json` (N=325) | `auc_ci(n_resamples=1500, seed=42)`; point estimate is `metrics.auc` (n_points=200) |
| §7.5 generalization matrix (full-N cells) | CASIA 0.9454 (N=2,408), CelebA 0.7820 (N=2,611), in-house 0.9264 (N=100) | the corresponding `*_minifasnet_only.json` files above | `metrics.auc` (full-resolution) |
| §7.6 latency | image_only 28.5 ms mean; hybrid 63.0 ms mean | `latency_image_only.json`, `latency_hybrid.json` (N=50 frames) | n/a — wall-clock measurement |
| §8.1 image-only vs hybrid (full-resolution AUC) | CASIA 0.9454/0.9140/0.9139; CelebA 0.7820/0.7262/0.7245; in-house 0.9264/0.9264/0.9245 | the `results_*_{minifasnet_only,image_only,hybrid}.json` files above | `metrics.auc` (full-resolution) |
| §8.2 LOO in-house replay | baseline ACER 14.03%, AUC 0.9497; per-analyzer Δ rows | `ablation_loo_in_house_replay_n100.json` (N=83) | point estimates from `classification_report` (no CI) |
| §8.2 LOO CASIA-FASD | baseline ACER 28.70%, AUC 0.7793; per-analyzer Δ rows | `ablation_loo_casia_fasd_test_n300.json` (N=300) | point estimates from `classification_report` (no CI) |
| §8.3 four weight configs (measured, one capture) | uniform 0.9190 / partial 0.9341 / calibrated 0.9424 / minifasnet-dominant 0.9552 (all N=83); uniform→calibrated Δ-AUC +0.0234 | `weightcfg_in_house_replay_n100_{uniform,partial,calibrated,minifasnet_dominant}.json` (each embeds per-sample analyzer scores + weight map + fused P(REAL)) | point estimates (no CI); see §8.3 environment note — calibrated AUC 0.9424 here vs 0.9497 in the §8.2 LOO baseline JSON (uniface/ONNX-runtime version difference; deltas computed within this self-consistent capture only) |
| §8.5 synthetic-flash smoke test | ACER 40.00%, AUC 0.5685, APCER 40.00% | `results_active_challenge_in_house_replay_active.json` (N=100) | point estimate (no CI) |
| §8.7 N-effect (CASIA subsamples) | N=200 AUC 0.840 [0.755, 0.910]; N=500 AUC 0.855 [0.810, 0.893] | `results_casia_fasd_test_{n200,n500}_minifasnet_only.json` | `auc_ci(n_resamples=1500, seed=42)` |

**Single-command reproduction.** Per the operator runbook (`RUNBOOK_PAPER_PREP.md`), each results JSON is regenerated by one `python -m tests.benchmark.run --dataset <name> --pipeline <p> --protocol <proto>` call; the leave-one-out JSONs by `python -m tests.benchmark.ablation_leave_one_out`; the four §8.3 weight-config JSONs by `python -m tests.benchmark.ablation_leave_one_out --mode weights` (one capture, four re-fusions); and the CIs by the `auc_ci` / `acer_ci` / `eer_ci` helpers in `src/metrics/bootstrap.py` with the resample counts above. Every row in §7–§8 is now backed by a committed JSON in `paper/figures/`.
